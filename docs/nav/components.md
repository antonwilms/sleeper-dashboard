### src/components/
| File | Responsibility |
|------|----------------|
| `shell/AppShell.jsx` | App frame: always-on `TopBar` (forwards `currentWeek`; also `searchablePlayers`/`popupOpen`/`onOpenPlayerDetail` for global search) + (post-league) desktop `NavRail` / mobile `BottomTabBar` + content area; pure chrome, owns no state; fixed explicit prop list — does not forward arbitrary props |
| `shell/navItems.js` | Nav config: `PRIMARY_NAV` (flat, for `BottomTabBar`), `NAV_GROUPS` (grouped MANAGE/LEAGUE, for `NavRail`), `LEAGUE_NAV`, `ROOKIES_NAV`, `DEFAULT_ROUTE`, `isRookieSeason()` |
| `shell/{TopBar,NavRail,BottomTabBar,CareerLoadProgressBar,ClearCacheButton,ExportDataButton}.jsx` | Shell chrome + extracted header/progress/utility components. `TopBar` (still on the `--color-*` token family, never `--color-dp-*`): logo + league name, freshness indicator (`currentWeek` prop), user/Switch, mobile League link, and a global `⌘K`/`Ctrl+K` player search (`searchablePlayers` prop, a narrow `{player_id, full_name, position, age, nfl_team, score}` projection from `App.jsx`; ≥2 chars opens a results dropdown, top 8 by `score` descending nulls-last, guarded substring match on `full_name`; picking a result calls `onOpenPlayerDetail(id)`). Owns the query/dropdown-open state and the `⌘K` listener itself (not `App.jsx`); inert while the pop-up is open (`popupOpen` prop = `tabs.length > 0`). Disabled when `searchablePlayers` is empty. `sm:hidden` below the `sm` breakpoint — desktop/tablet only, by design; Market's own text filter covers mobile. There is no tooltip subsystem — the toggle button and the `Tooltip.jsx`/`TooltipContext.jsx` components it drove were removed; tooltips may return as a designed feature in a future slice |
| `league/{LeagueView,StandingsTable,ScheduleGrid,RostersTab,SlotBadge}.jsx` | Secondary "League" group surfaces. `LeagueView`'s own sub-nav is `md:hidden` — desktop reaches Standings/Schedule/Rosters via the rail's LEAGUE group; mobile still needs the in-page tabs |
| `week/WeekView.jsx` | Route container for `/week`, "This week" — props-only, exactly as `/teams` is. Resolves the user's roster (`rosterTeams.find(t => t.teamName === myTeamName)`) and the gated live-season schedule (`nflScheduleByYear?.[season]`, passed to the hook only when `.complete`), calls `useWeeklyDecision`, and stacks `WeightPanel` beside the header, `StoreLagNotice` and `LineupTable` (starters + bench) beneath it. No `vs {opponent}` clause in the header — a deliberate v1 omission (`leagueData` carries no schedule for the current week's league matchup), not an oversight |
| `week/WeightPanel.jsx` | "How much of this is {season}" — one row per `blendWeights.js`'s `SIGNAL_FAMILIES` entry: label, bar, percent, then `k {k} · all {dropGames} gm` or `k {k} · all wk {dropWeek}` (never both, never a fabricated conversion between them — the row carries exactly one of the two fields). Header's right-hand text gains `· STORE THROUGH WK {k}` when `storeLag?.behind`. Presentational, props-only |
| `week/LineupTable.jsx` | Starters as set in Sleeper, slot by slot, then a `BENCH · {n}` divider (only when non-empty) and the bench: SLOT/PLAYER, OPPONENT DEFENCE (VS + ALLOWS with its blend-weight bar, coloured in coarse bands at rank ≥27/≥23/≤6/≤10/else — never a gradient), USAGE (RUSH/TARGET/TOUCH/SNAP, each a real percentage or `—` for a genuine non-observation — SNAP renders `0%`, not `—`, for a player who played but took no offensive snaps), a last-3-played-weeks form sparkline (void slots dashed, never a filled zero), and PROJ (`—` for `points == null`, never a fabricated `0.0`). An empty starter slot renders the muted word `Empty`. `role` and the opponent's W-L record have no source in this slice and render nothing (`PROVISIONAL(no-data)`, at the render site), same for each usage share's "last season" sub-line the design mock shows in grey. Presentational, props-only |
| `week/StoreLagNotice.jsx` | Renders nothing unless `storeLag?.behind`; one muted line naming both `storeThroughWeek` and `completedWeeks` (a second copy variant when `storeThroughWeek === 0`). Presentational, props-only |
| `portfolio/Portfolio.jsx` | My Team screen (`/portfolio`, Slice B/C): header (title, meta line, summary sentence, three lineup tiles), a Starting ten table, the League ladders + Weakest slots row (Slice C — `LeagueLadders.jsx`/`WeakestSlots.jsx`, between Starting ten and Bench), and a Bench table. Slice D adds the `TeamOffences.jsx` block between the ladders row and Bench (rows assembled here from `teamContextByYear`/`gameLogsByYear`/`nflScheduleByYear`/`currentSeasonTotals`, all four gated on `complete`; absent unless teamContext is loaded). Props-only like Market — six props added over the prior screen (`careerStats`, `playerMap`, `rosterPositions`, `scoringSettings`, `leagueName`, `username`), `ktcHistory` removed (the deleted tiles were its only reader). The lineup itself comes from `utils/lineup.js`'s `buildLeagueLineups`/`buildPositionLadders`/`buildSlotMedians`/`startingBar`/`buildWeakestSlots`, run on `dataSeason` (`environment.js`'s `deriveDataSeason(careerStats)`, never `nflState.season`) — `myLineup` is the `proj`-side best lineup for the user's roster, in `rosterPositions` slot order. Per-column sources: `SNAP` and `SHARE` read `utils/outlookUsage.js`'s `buildUsageHistory` (the same call Market's Outlook set and the pop-up's usage section make, over a locally-built `perSeasonTeamShares`, never the projection-pipeline `historicalShares` series — that would let this screen disagree with the pop-up opened from the same row); `STATUS` and `ROLE` render live `playerMap[id]` fields (`injury_status`/`injury_body_part`, `depth_chart_position`/`depth_chart_order`) raw, display-only, never captured or scored; `POS RANK` and last-season PPG come from `utils/seasonRanks.js`'s `rankPositionSeason` over `careerStats[dataSeason]`; `GAMES` reads served `weeklyStatus` via `utils/availabilityGrid.js`'s `buildAvailabilityGrid`, drawing both `'B'` and `'X'` as a dashed "bye or no game" cell. `GAME SCRIPT` (Slice D, both tables) is `utils/gameScript.js`'s descriptor for the row's team (`normalizeTeamForSchedule(nfl_team)`, so `LAR` lands on `LA`), coloured by `gameScriptFit` for that player's position; a free agent, a team with no metrics, or no teamContext renders `—`. `VS MEDIAN STARTER` (Bench only) is the gap to `startingBar`'s lowest eligible league-median slot. Picks render as Bench rows via `PickCell`/`PickValueCell` (unchanged from the prior screen: `own pick` / `via <original owner>` meta, KTC `Mid`-tier pricing with `Early`/`Mid`/`Late` in a `DefinitionPopover`, round 5+/unpriced renders a dashed `—`, never `0`) — but pick rows are plain `<tr>`s, not `ClickableRow` (which hard-codes `onOpen(row.player_id)`; a pick has no `player_id`). Reconstructs pick ownership via `utils/tradedPicks.js`'s `deriveLiveSeasons`/`reconstructPickOwnership` (live seasons are the *intersection* of "KTC prices this season" and "≥ first-live", so a season whose draft already happened is excluded rather than double-counted against players already rostered), unchanged from the prior screen. Bench sorts projected-players desc, then unprojected players by `ktcValue` desc, then picks by `ktcValue` desc, collapsing past 10 rows behind a `show all N →` toggle. `SLOT_LABEL`/`slotLabel` now live in `portfolio/slotLabel.js` (Slice C, C10) — `Portfolio.jsx` imports rather than defines them |
| `portfolio/TeamOffences.jsx` | Portfolio Slice D — "The offences your starters play in". Props-only (`rows`, `dataSeason`, `sosSeason`, `rankedTeamCount`, optional `fpaCurrentSeason` for the SOS popover's blend wording); computes nothing from loaders. 12 columns: TEAM, YOUR PLAYERS, PTS/G (inline `<div>` bar), PTS ALLOWED, MARGIN, OFF EPA/PL, PROE, PLAYS/G, RZ TRIPS/G, DEF EPA ALL, QB · EPA/ATT, SOS. Rank tone is blue top-8 / amber bottom-8 (`rankedTeamCount - 7`, never a literal 25); SOS and DEF EPA ALL are inverted at the render site; MARGIN is deliberately uncoloured and PROE is never inverted per roster (both explained in the file header). Starters' teams first, bench-only teams behind a `show all N →` button. Shares `TH_CLASS`/`DIVIDER` with `Portfolio.jsx` via `tableClasses.js` |
| `portfolio/tableClasses.js` | `TH_CLASS` / `DIVIDER` header-cell classes shared by `Portfolio.jsx` and `TeamOffences.jsx` — a pure move (like `slotLabel.js`) because a back-import from `Portfolio.jsx` would be a cycle |
| `portfolio/slotLabel.js` | Starting-slot display labels (`SLOT_LABEL`: QB/RB/WR/TE unchanged, FLEX→`FLX`, SUPER_FLEX→`SF`) and the `slotLabel(s)` lookup, shared by `Portfolio.jsx`'s Starting ten table and `WeakestSlots.jsx`'s row labels (Slice C). Pure, imports nothing |
| `portfolio/LeagueLadders.jsx` | Portfolio Slice C — "Where you rank, out of twelve". Renders `utils/lineup.js`'s `buildPositionLadders` output as a rank ladder per position (`QB`/`RB`/`WR`/`TE`/`Lineup`), rungs = `teamCount`. Defines its own `rankTone`/`rungFill` helpers rather than reusing `Portfolio.jsx`'s `rankClass` — the design's neutral rank colour for this block (`text-dp-text`) differs from the tiles' (`text-dp-text-5`), and sharing one helper would silently restyle the tiles. Tone/fill boundary is `third = Math.ceil(teamCount/3)` (generalises the design's fixed 12-team `≤4`/`≥9` split). Geometry is rank-only — `lastAll`/`projAll` stay unread, never used for bar-position math. MOVE cell reads Slice A's already-signed `move` (negative = better) with no re-derivation. Props-only presentational component, no domain state |
| `portfolio/WeakestSlots.jsx` | Portfolio Slice C — "Weakest slots, in points". Renders `utils/lineup.js`'s `buildWeakestSlots` output unsliced; owns the `WEAKEST_ROW_LIMIT = 4` row cap and computes its one-paragraph summary sentence over the rendered (capped) rows only — never the full `rows` prop. Row labels are built once from the `slots` prop: a slot type occurring more than once gets a 1-based occurrence suffix (`RB`×2 → `RB1`/`RB2`); `slots.length === 0` is the "no lineup loaded" case, distinct from `rows.length === 0` with populated `slots` (nothing losing) — two different summary sentences, both rendered as the single `weak-summary` paragraph. Bar widths are scaled by the largest rendered `median` (never a literal constant), clamped so `mineW + lossW ≤ 100`. Props-only presentational component, no domain state |
| `market/Market.jsx` | Market table over `playerRowsWithProj`: Value/Outlook/Volume/Efficiency column-set switch (`market-column-set`; a stored `'production'` migrates to `'volume'` on read and is written back, an unrecognised value falls back to `'value'`; the two-group set control is `MODEL & MARKET` [Value][Outlook] · `ON FIELD` [Volume, Efficiency]), position pills, sort (`usePlayersTable`, `market-sort`; see `src/hooks/` table for the three-set sort-reset mechanics), pagination, row click/keyboard → `onOpenPlayerDetail`. A persistent `TREND` gutter sits right of `PLAYER` in every set — `dp/TrendCell` at `scale="cell"`, delta/window/band read from `seasonProjections[id].factors` (`ktcHistDelta`/`ktcHistWindowSpanDays`/`ktcHistConfidence`, merged upstream by `seasonProjection.js:307`, never recomputed here), sparkline from the `ktcHistory` prop (`series[id]` entries mapped to `.value`). Sorts via a comparator branch (`_trend`, reading `.delta`) in all three sets, following the `_snapTrend`/`_oppTrend` precedent — a bare-key sort on an object silently no-ops; `colSpan` is bumped by 1 in all three sets. `market-production-season` (Volume's season selector) is deliberately not renamed — the read falls back to the season list's first entry, so a renamed key would silently drop every stored season; `market-sort` needs no migration (its payload carries no set key). The Outlook set's Snap/Opp trend cell component is `UsageTrendCell`, distinct from the imported `dp/TrendCell` primitive. Column *derivations* are harvested (imported, not copied) from `outlookPositionStats`/`outlookUsage`/`outlookConsistency`/`nflStats`/`dynastySignalBadges`/`seasonRanks` (Ceiling/Floor), plus `market/columnDescriptors.js`'s `COLUMNS`/`POSITION_STAT_COLUMNS` descriptor maps. EFFICIENCY is per-position (QB / RB / WR-TE, ~5 columns each), a single season pinned to `dataSeason` (the season `<select>` stays hidden, unlike Volume's user-selectable one; the header states the fixed season and the gp≥8 limitation rather than hiding it). Columns: QB `EPA/ATT`/`CPOE`/`SACK%`/`AY/ATT`/`RUSH EPA`; RB `CARRY SH`/`TGT SH`/`RUSH EPA/ATT`/`YAC`/`BTKL`; WR/TE `TGT SH`/`AY SH`/`aDOT`/`EPA/TGT`/`RACR`/`RZ SH`/`SNAP%`/`DROPS` (`ALL` reuses WR/TE's list). Three derivations back it: `src/utils/seasonEfficiency.js` (one-pass gamelogs aggregator, memoised on the loader result + `dataSeason`, REG-only, sum-then-divide; `CPOE` is attempt-weighted, `Σ(passingCpoe × attempts) ÷ Σattempts`, since it's a per-game rate and a plain mean would let one low-attempt game dominate; `CARRY SH` is its one cross-family join, `Σ carries ÷ Σ teamcontext.off.rushPlays` matched `(team, week)` — deliberately wider than the per-season-team rush share elsewhere, since `off.rushPlays` includes QB scrambles/sneaks; its `resolvePlayerTeam` calls are pre-filtered to REG games, since that helper's week-grain lookup is seasonType-blind); `RACR` from the `advStats` prop (`byId[id].racr`, gated on `complete`) rather than re-derived from gamelogs; and `TGT SH`/`AY SH`/`aDOT`/`RZ SH`/`SNAP%` reuse `buildPositionStatSeries`/`buildRzShareSeries`/`buildUsageHistory` but take the latest entry only when its season equals `dataSeason` — those helpers gate at `gamesPlayed≥8` (shared with the projection's attribution, never forked), so all five render `—` for roughly the first half of every season, stated in the header rather than worked around. `METRIC_META` (`usageEfficiency.js`) is the single metadata source for all of it. Column-set switches and position-pill clicks while Efficiency is active re-assert that POSITION's own lead metric (`EFFICIENCY_LEAD_SORT`: QB→`EPA/ATT`, RB→`CARRY SH`, WR/TE→`AY SH`) via the `setSortState` escape hatch — `DEFAULT_SORT` alone can't express a per-position default, only a per-set one. Four props (`gameLogsByYear`/`teamContextByYear`/`historicalTeamTotals`/`advStats`) feed Efficiency — Market stays props-only (growing the explicit list is the accepted cost, not a signal to switch to `ProfileDataContext`). Four environment filters (Team PROE/pace/off. EPA-play/RZ TD rate) live in the filter panel — `rankTable`, a `useMemo(() => buildLeagueRankTable(teamContextByYear[dataSeason], FILTER_METRICS))` gated on that season's `complete` flag (not key presence — an incomplete load yields empty per-metric maps that would otherwise empty the table the moment a control moved), keyed to `dataSeason`, not the user-selectable `activeVolumeSeason` (the wrong one would silently re-join the filters to whatever season Volume's selector holds). Passed into `applyMarketFilters`'s widened ctx alongside `careerStats`/`season` for the `resolvePlayerTeam` join. See `marketFilters.js`'s row for the predicate/preset details. Dark-only `--color-dp-*` throughout; does not consume `ProfileDataContext` (gets its data as props from `App.jsx` — `careerStats`/`playerMap`/`seasonProjections`/`ktcHistory`, the last feeding the TREND gutter's series only). Its presentational cells (`CareerBars`/`PlayerCell`/`ClickableRow`/`DeltaCell`/`SortTh`) live in `dp/cells.jsx`, shared with `Portfolio.jsx`; `CeilingFloorCell` stays local to Market, on dp tokens, with no tier-colored rank badge (that badge's raw `--c-*` primitives depended on a light/dark toggle the app no longer has; restoring the badge is possible but not done). `filters` (view-local, `market-filters`, validated on read via `marketFilters.normalizeFilters`) applies in `displayRows` after the position pill and before sort/pagination; the header subline and `MarketTable`'s pager both read the same filtered array, so all three counts on screen agree. Free-text search + saved presets are covered by `market/FilterBar.jsx`'s row. The Value set carries `KTC`/`Ceiling`/`Floor` columns — the raw KTC value and the best/worst single-season positional finish |
| `market/FilterBar.jsx` | Active-filter pills (each with its own `×`), a free-text filter input ("Filter by name…", left of "+ Add filter"), "+ Add filter" (toggles the panel it also renders), saved presets (`market-filter-presets`, cap 5, re-saving an existing name works at the cap), and "Reset all" — the presets control and "Reset all" share one `ml-auto` wrapper (visible whenever presets exist or a filter is active), not two independent `ml-auto` siblings. Owns the panel's open/closed UI state AND the presets list (load-filter-via-`isRestorableFilters`-at-mount/save/apply/delete) — both view-local, single-consumer chrome state; filter *values* are `Market.jsx`'s, passed down as `filters`/`onFiltersChange`. `buildPills` has four clauses for the environment filters (`PROE top N` etc.) — a hand-written per-key chain like `activeFilterCount`, not data-driven, so a new key needs an explicit clause here (unlike `clearOne`/`resetAll`, which read `DEFAULT_MARKET_FILTERS[key]` generically and need no edit). Rendered by `Market.jsx` and passed into `dp/MarketTable.jsx`'s `filterBar` render-prop |
| `market/FilterPanel.jsx` | The expandable filter grid (`bg-dp-card`, 4-column), on the dp token language. Range-slider bounds are read directly from `marketFilters.DEFAULT_MARKET_FILTERS`, so they can't drift from the sentinel defaults the predicates gate on. Filters apply live; Apply and Reset both collapse the panel (Apply is a close-only action, not a commit point). An Environment group holds four `SingleSlider`s (not `RangeSlider`, which is two-handle and would destructure a scalar to `undefined`), `min={1} max={LEAGUE_TEAM_COUNT}` (derived from `NFL_TEAMS.length`, not a second `32` literal) — for the one dimension whose *off* state is its *maximum*. `SingleSlider` takes a `format` prop (`envTopFormat`: "top N of 32" / "any" at the ceiling) so `minProjectedGames`'s bare-number render stays untouched |
| `teams/Teams.jsx` | The 32-team index (`/teams`). Zero new fetching — reads `teamContextByYear[dataSeason]` plus `playerRows`; `dataSeason` via `environment.js`'s `deriveDataSeason(careerStats)` helper, not a local copy of Market's inline derivation. Columns: `TEAM · PROE ↓ · PACE · SUCC% · OFF EPA/PL · RZ TD% · DEF EPA ALL · PTS/G · YOUR EXPOSURE`, default sort PROE descending, one `computeTeamSeasonMetrics` pass per team via `buildTeamMetricsTable` — the 32 rows are driven by that loaded season's own `teams` keys (era-accurate domain), not `marketFilters.NFL_TEAMS` (Sleeper domain, carries `LAR`). `DEF EPA ALL` is the one inverted column — first click sorts ascending (best defences first, in `usePlayersTable`'s `ascByDefault` set) and colours negative blue / positive amber, the reverse of `OFF EPA/PL`; `LOWER_IS_BETTER` in `environment.js` is deliberately untouched (its only consumers, `computeLeagueStanding`/`buildLeagueRankTable`, are not called here — the surface owns its own direction and colour). `YOUR EXPOSURE` (why the surface exists) scopes to `ownerTeamName === myTeamName` and buckets each row on `normalizeTeamForSchedule(nfl_team)`, never raw `nfl_team` (`playerMap[id].team`, the Sleeper domain — CR-16's boundary; ungated, every `LAR` player would land in an unmatched bucket and `LA` would silently read `none`); the literal string `'FA'` (a real free-agent asset) counts in the value denominator only, so shares sum to ≤100% and are never rescaled to 100%. A team with nobody owned renders `none`/`—`, muted, not `0 players`/`0%`; a null `ktcValue` is skipped in both the numerator and denominator, not zeroed; `myTeamName` null renders the whole column `—`. A distribution strip (`dp/SeriesBars.jsx`) redraws under the header for the currently sorted column — passes `colour="neutral"` (a signed metric's raw sign is not colour-coded here, since `DEF EPA ALL`'s own inverted cell colours would otherwise contradict it). Takes `loaded` like Market/Portfolio (distinct loading vs. degraded states) — `loaded===false` is the loading state; `loaded===true` with the season's teamContext absent/incomplete is one whole-surface `dp/DegradedBlock` (`kind="not-yet-accruing"`, named explicitly — an unnamed `kind` falls through to an empty label). Rows are clickable — whole-row + keyboard (Enter/Space), navigating via `useNavigate()` through a component-scoped `goToTeam` callback (not module-level, since hooks can't be called outside a component) — never a raw `window.location.hash` write, which is invisible to the router and would go unseen by a future route guard or navigation instrumentation; `Teams.test.jsx`'s renders are wrapped in `MemoryRouter`. `buildExposure`/`exposureForTeam` live in `src/utils/teamExposure.js` (team detail's holdings block needs the same computation). Four FPA columns — `FPA QB`/`FPA RB`/`FPA WR`/`FPA TE`, appended after `PTS/G`, blended per-game fantasy points allowed via `src/utils/opponentStrength.js`'s `buildFpaTable`/`rankFpaTable` (one pass over the DEF rows, independent of `teamContextForSeason` — computed even before that season resolves). Joined onto the era-accurate row set via `normalizeTeamForSchedule`, same domain hop as `YOUR EXPOSURE` above. No colour on the cells (a soft defence is good for your starter, bad for your own DST — polarity is stated in the per-column `DefinitionPopover`'s text only); ascending-first via `usePlayersTable`'s `ascByDefault` (rank 1 = toughest). Each popover cell stops click/keydown propagation — it sits inside the whole-row navigate-to-team-detail handler and would otherwise also navigate on open. `currentSeason` is read straight off the `currentSeasonTotals` prop's `complete` flag — the App-level `loadCurrentSeasonTotals` loader's own single answer to "is the live season available," not a second local derivation (an independent manifest-entry-based gate would risk disagreeing with the fetch/validator/schema-ceiling's own answer). Deliberately different from `dataSeason` (see `opponentStrength.js`'s own row); when `currentSeasonTotals.complete` is false — the manifest entry for the live season's `nfl/season-totals/<year>.json` is null (file missing, store disabled, or manifest fetch failed), or `tryDataStore` itself returned null (validator or schema-ceiling rejection) — the popover states the prior season alone rather than implying a blend. API-only mode (`VITE_DATA_STORE_URL` unset) serves zero DEF rows (the live-API fallback filters on `activePlayerIds`, and DEF entries carry `status: null`) — detected by the built FPA table being empty, rendered as a stated muted note above the table, never a silent blank column |
| `teams/TeamDetail.jsx` | `/teams/:abbr` team detail. Reads `:abbr` itself via `useParams` (`LeagueView.jsx` is the precedent; `App.jsx` stays route-unaware) and calls the `onNeedTeamHistory` prop once on mount to trigger the on-demand 14-season load (see `useTeamHistoryLoader.js`). Four metric cards (`PROE`/`PACE`/`SUCCESS RATE`/`OFF EPA / PLAY`) — current-season value + league median/rank (`computeLeagueStanding`) + a percentile bar derived from real `rank`/`n` (never fabricated p10/p90 — `computeLeagueStanding` returns neither) + a 14-season `SeriesBars` chart + a direction label + an axis label (`scaled` cards state their floor, e.g. `AXIS 0–100%`; `signed` cards read `ZERO BASELINE`) + the field expression. The 14-season lookup resolves every season through `eraTeam(abbr, season)` (`utils/playerTeam.js`) — teamcontext is keyed era-accurately, so a fixed `LA` lookup finds nothing for 2012–2015 (real key is `STL`) and would render four void slots as though data were missing; same for `LAC`→`SD` (≤2016) and `LV`→`OAK` (≤2019). Renders whatever seasons are loaded without gating — a chart with 5 of 14 seasons present still renders, with `SeriesBars` void-slotting the rest. Signed-mode cards pass `colour="neutral"` — a metric whose sign isn't a verdict must not editorialise. Holdings ("Rostered & tracked", explicitly NOT framed as a full depth chart — `playerRows` is relevance-gated) filter on `normalizeTeamForSchedule(nfl_team) === abbr`, sorted by `ktcValue` descending nulls-last; per-row meta reads `N% of roster` (`dp-up-text`) for the user's own players, `owned by <manager>` (muted) for another manager's, `not owned` (muted) otherwise — the roster-value denominator reuses `teamExposure.js`'s `buildExposure`, not a second sum. Coaching resolves two silent domain/year traps: `enrichment/coaching.json` keys the Sleeper domain (`LAR`), so the route param is denormalised via `nflStats.js`'s `denormalizeTeamForSchedule` before calling `getCoaching`; and every coaching entry is a fixed future year (not `dataSeason`), so the surface derives the enrichment's own year locally (max `year` for the resolved team, falling back to the max across all entries) and labels the block with it. An empty coaching payload for the resolved year renders `DegradedBlock` (`kind="no-baseline"`). Three states, matching `Teams.jsx`'s own gate exactly: `loaded===false` → loading; `loaded===true` with the current season's teamContext load absent or incomplete → `DegradedBlock` (`not-yet-accruing`); `loaded===true` + abbr absent from that season's `teams` → `DegradedBlock` (`never-available`) — an unknown/historical `:abbr` (e.g. `/teams/STL`) lands here, not a crash or redirect |
| `dp/cells.jsx` | Shared dp-styled presentational cells: `SortTh`, `PlayerCell`, `ClickableRow`, `CareerBars` (5-wide sparkline, dimensions fixed — do not respec per caller; void slots — `null` renders a dashed baseline marker, distinct from a measured `0`'s filled 2px stub), `DeltaCell` (colored ±N text). Imported by both `Market.jsx` and `Portfolio.jsx`. Carries the zero-based-vs-min–max normalisation rule (also in `dp/SeriesBars.jsx`) as a file-header comment |
| `dp/CoveragePips.jsx` | Three-pip coverage indicator (`band` or raw `count`, converted via `coverageBand.js`); no colour under any prop, decorative (`aria-hidden`). Rendered by `dp/DistributionSection.jsx`, pips over the pooled **game** count |
| `dp/SeriesBars.jsx` | Arbitrary-length sibling to `CareerBars`; never pads, never substitutes `0` for a null. `'scaled'` (min–max, optional explicit `domain`) or `'signed'` (real zero axis, positives above/negatives below a 1px rule) modes. Distribution's histogram draws its own bars instead — a bucket count of `0` is itself a void slot there, not merely a null entry, which fits neither mode. Consumed by `dp/UsageEfficiencySection.jsx` (`'scaled'` mode with an explicit domain, one call per metric row), `dp/EnvironmentSection.jsx`, `teams/TeamDetail.jsx`'s two signed 14-season cards, and `teams/Teams.jsx`'s distribution strip. Takes a `colour="neutral"` prop (default unchanged) — `'signed'` mode otherwise colours by raw sign (`bg-dp-up`/`bg-dp-down`), which editorialises a metric whose sign isn't a verdict (negative `DEF EPA ALL` is good, positive `OFF EPA/PLAY` is good); `'scaled'` mode is unaffected, already neutral. `teams/TeamDetail.jsx` and `teams/Teams.jsx` pass `colour="neutral"`; `dp/EnvironmentSection.jsx`/`dp/UsageEfficiencySection.jsx` keep the default colouring — regression-guarded by `seriesBarsColourRegression.test.js` |
| `dp/TrendCell.jsx` | Series → delta (glyph + colour) → window label primitive, three geometries (`cell`/`tile`/`section`) via a lookup object. Band-gated (`low` suppresses the series; `none` renders `—`; unrecognised treated as `none`). Rendered by `market/Market.jsx`'s TREND gutter (`scale="cell"`) — distinct from Market's own module-local Snap/Opp trend cell, `UsageTrendCell` |
| `dp/DefinitionPopover.jsx` | Click-triggered (never hover) definition popover: term/scope, gloss, percentile strip, coverage pips, field expression. Keyboard-operable (`Escape` closes, focus returns to trigger); one open at a time via local state, no context/provider. Rendered by `dp/UsageEfficiencySection.jsx`, one per metric row (term/gloss/field expression only — no percentile strip, that data isn't computed here) |
| `dp/DegradedBlock.jsx` | The five degraded-data kinds (`not-yet-accruing`/`not-measured-then`/`undefined-here`/`never-available`/`no-baseline`); `never-available` alone gets the amber border/label pair, the rest neutral. Never a call to action. Rendered by `dp/GameLogSection.jsx`/`dp/DistributionSection.jsx` (both `not-yet-accruing`/`not-measured-then` only) |
| `dp/GameLogSection.jsx` | The pop-up's `§game-log` body: one row per game the player's team played in `mostRecentSeason`, REG (weeks 1–18, driven by `careerStats[...].weeklyStatus`) then POST (enumerated from the gamelogs family directly). Per-position production columns (`src/utils/gameLog.js`'s `GAME_LOG_COLUMNS`, `TE` aliases `WR`); `RESULT` derived from the schedule's home-margin field, not printed verbatim; `PTS` from `weeklyPoints[week]` (league-scored), never the gamelogs nflverse figure. Degrades to `DegradedBlock` when the season's `gameLogsByYear`/`nflScheduleByYear` aren't both `complete`, or the player has no `games[]` |
| `dp/DistributionSection.jsx` | The pop-up's `§distribution` body: a per-game fantasy-points histogram (`src/utils/distribution.js`'s 5-point buckets plus an explicit `<0` bin) pooled over `computeConsistency`'s own season window (reused, not re-derived, so its SD is provably the Overview tile's `±SD`), a shape block (mean/SD/CV/over-20/under-10), and `±1 SD` dashed markers positioned via `bucketAxisPercent`. Coverage pips read the pooled game count, not the season count. Degrades to `DegradedBlock` when `computeConsistency` returns `null`; a non-null result with a null `sd` still draws the histogram, with the SD row and markers omitted |
| `dp/MarketTable.jsx` | Dp-styled presentational table shell for Market: `header`/`renderRow`/pagination render-prop shape, `PAGE_SIZE=50`. Hard-codes the ALL/QB/RB/WR/TE pill row and always renders a pager, so it cannot be reused as-is by Portfolio (no pills, no pager) — Portfolio's table shell is inlined in `Portfolio.jsx` instead. Takes an optional `filterBar` render-prop, rendered in the SAME flex-wrap row as the position pills — absent prop renders with no filter bar |
| `dp/SectionIndex.jsx` | 140px table-of-contents column for the pop-up body (`{ sections, activeId, onSelect }`). One `<button>` row per section; clicking scrolls (never swaps/unmounts/filters), no route/hash. Renders `CoveragePips` only when a row carries a `count` — with neither `band` nor `count`, `CoveragePips` renders three unfilled pips, a visible artefact this component avoids by omitting the element entirely at sites with no real coverage. Hidden below the `dpwide:` breakpoint (`--breakpoint-dpwide` token, `src/index.css` `@theme`) |
| `dp/PlayerDetailModal.jsx` | A non-scrolling row (`min-h-0`, mandatory: without it the row grows to content height under the panel's `overflow-hidden` and the inner scroll column never overflows) of `dp/SectionIndex.jsx` beside a single `overflow-y-auto` scroll column holding eight sections, in JSX order (`§overview` — identity row/tiles/chart and the right rail, `§game-log`, `§distribution`, `§usage`, `§availability`, `§environment`, `§drivers`, `§why-next`); reads `usePlayerProfile` + the App-level `ProfileDataContext.Provider`, incl. `teamContextByYear`/`historicalTeamTotals`; dark-only `--color-dp-*`. `SECTIONS` (ids/labels) is a module-level const read by the section wrappers and must be kept in this same order — it drives the index and the `IntersectionObserver` scroll-spy, but the rendered order is the JSX order below it, so the two are edited together, never independently; a `useMemo` decorates it per-player with Overview's coverage count (`dynastyScore.signals.seasonsOfData`, reused rather than a second "seasons of data" definition) for the index. Active-section highlight is `IntersectionObserver`-driven (root = the scroll column, `rootMargin` shrunk to a top band so a tall section like Overview doesn't outstay its welcome); clicking an index row also calls `scrollIntoView`. The right rail has no `overflow-y-auto` of its own (dead once scoped inside a scrolling section) and is `w-full dpwide:w-[300px]` — stacks below `§overview`'s content under 1180px, per the `dpwide:`/`max-dpwide:` variants (never an interpolated `max-[1180px]:` — see the file's own header comment). Section labels render in the index only; the existing card titles remain the sole visible headings. The `!dynastyScore` empty-state branch renders no index, no scroll column. Takes `{ playerId, myTeamName, onCompare }` — no `onClose` (the scrim/panel/close-× live in the shell). Rendered by `dp/PlayerDetailTabs.jsx` for whichever tab is active. `§game-log`/`§distribution` are `dp/GameLogSection.jsx`/`dp/DistributionSection.jsx`, fed `careerStats`, `gameLogsByYear[mostRecentSeason]`, `nflScheduleByYear[mostRecentSeason]`, and the already-computed `consistency`. `§usage`/`§availability` are `dp/UsageEfficiencySection.jsx`/`dp/AvailabilityRoleSection.jsx` (own rows below), fed `careerStats`, `playersMap`, and a shared `axisSeasons` (`careerHistory.slice(-5)`'s seasons — computed once here so both sections' per-season rows align); `§usage` also takes `historicalTeamTotals` (for its red-zone row); `§availability` also reads the hook's `teamDepthChart`. `§environment` is `dp/EnvironmentSection.jsx` (own row below), fed `careerStats`, `teamContextByYear` and its own axis, `envAxisSeasons` — computed here too but deliberately NOT `axisSeasons`: it's `Object.keys(careerStats).sort().slice(-5)`, the last 5 league seasons (the same window `App.jsx`'s loader effect requests), not the player's last 5 seasons with data — for an older player those sets are disjoint. `src/utils/dynastySignalBadges.js` derives the SIGNALS-rail content; Market's Outlook column set is the helper's second consumer, re-applying a `0.95–1.05` age-curve gate locally rather than editing the shared helper |
| `dp/UsageEfficiencySection.jsx` | The pop-up's `§usage` body. Renders EXISTING derivations only — `outlookPositionStats.buildPositionStatSeries` (per-position metric set: QB `cmpPct`/`passerRating`/`sacks`; RB `rushShare`/`rbTargetShare`/`yardsPerCarry`; WR/TE `targetShare`/`airYardsShare`/`aDOT`) plus `outlookUsage.buildUsageHistory`'s `snapPct` for RB/WR/TE only (QB is deliberately excluded, not widened — `usageMetrics.js`'s `SNAP_POSITIONS` reasoning; the same reasoning excludes QB from the red-zone row). Recomputes `buildTeamShareTotals`/`buildPerSeasonTeamShares` over the whole `careerStats` corpus itself (`Market.jsx` already does the same whole-corpus derivation independently, an accepted duplication). Every metric — including the red-zone row, `src/utils/usageEfficiency.js`'s `buildRzShareSeries(careerStats, playerId, position, historicalTeamTotals)`, player-season-total ÷ `historicalTeamTotals`'s unbiased team-season-total — is projected onto the caller-supplied `axisSeasons` (`alignToAxis`/`buildMetricRow`) so every row shares one x-axis and a gap season is a `null` void slot, never a collapsed/absent bar. `historicalTeamTotals` is an explicit prop; this component stays props-only with a fixed list, never a spread. `SeriesBars` in `scaled` mode with an explicit `[0,1]`/`[0,100]`/`[0,158.3]` domain for bounded fraction metrics (a share series is unreadable auto-scaled); unbounded count/ratio metrics (`sacks`, `yardsPerCarry`, `aDOT`) stay on auto min–max, since a guessed ceiling risks clipping a real outlier. `DISPLAY ONLY` badge on the section |
| `dp/AvailabilityRoleSection.jsx` | The pop-up's `§availability` body, two of the design's three blocks. Games-played grid: `src/utils/availabilityGrid.js`'s `buildAvailabilityGrid`, all four `weeklyStatus` codes (`P`/`D`/`B`/`X`) — `B` (bye) is only ever seen in API-only mode (`VITE_DATA_STORE_URL` unset), since store-served `nfl/season-totals` never emits it (real byes land in `X`, labelled "no game recorded" with a note); the legend lists `B` only when the rendered window actually has one. Never reconstructs byes from the schedule (season-grain team is a single dominant team per season — CR-02 — so a traded player would get phantom byes). Depth chart: the hook's `teamDepthChart` (not `depthChart`), the subject's own position group only, subject row marked; an empty/missing group renders a `DegradedBlock` (`no-baseline`). The design's third block, a weekly status strip sourced from Sleeper players-state snapshots, is omitted outright — no app loader exists for that family (capture-only in the data repo) — and gets no `DegradedBlock` standing in for it, since the degraded kinds describe states of data, not of implementation. `roleRank`/`usageShare` are left dark rather than given an invented home here |
| `dp/EnvironmentSection.jsx` | The pop-up's `§environment` body; the family's first rendering consumer of teamcontext's `off.*`/`def.*` shape anywhere in `src/` (CR-10). Four series metrics (`src/utils/environment.js`'s `SERIES_METRICS`), each a REG-only, components-summed-then-divided season aggregate (`computeTeamSeasonMetrics`) resolved per season via `resolvePlayerTeam` on its own axis (`envAxisSeasons`, the loaded league window — not `§usage`/`§availability`'s player axis; see the `PlayerDetailModal.jsx` row). PROE is the difference `(passPlays÷plays) − (proeXpassSum÷proePlays)`, `signed` mode; Pace, Success rate, Red-zone TD rate are `scaled`. League median/rank (`computeLeagueStanding`) come from the same loaded season's full 32-team `teams` object — no extra fetch — and pace's rank is computed lower-is-better while the other three are higher-is-better. A shared "Team by season" caption labels the per-season team once for the whole section (a traded player's bars span franchises). A splits block (current season only, not a series): off EPA/play with pass/rush split, plays/game, points/game, and own-defense EPA allowed (polarity noted in text, never coloured — negative is good defense, which is bad for the player's own volume). Whole-section `DegradedBlock` (`not-yet-accruing`) when the player's team resolves in none of the loaded seasons; a season with no resolved team, or with `teamContextByYear[season]` absent/`complete!==true`, is a per-metric void slot instead. `DISPLAY ONLY` badge; guarded by `teamContextViewOnly.test.js` |
| `dp/PlayerDetailTabs.jsx` | The pop-up's shell: scrim, panel, tab strip (up to 4 open players, FIFO eviction at the cap — `App.jsx`'s `tabs[]`/`activeTab`), the compare matrix (≥2 tabs, seven rows, winner-relative colouring per open tabs — not the mock's absolute thresholds), and the "+ Add player to compare" dropdown (top 5 by dynasty score, no text input, reuses `dp/cells.jsx`'s `PlayerCell`). Owns one local flag (`dropdownOpen`); a single `Escape` listener branches on it (closes the dropdown if open, else the whole pop-up — two listeners on `window` can't express "dropdown only", since `stopPropagation` between them is a no-op). Sources all per-tab data through `useProfileData()` — never `usePlayerProfile`, a per-`playerId` hook that can't be called once per open tab; `seasonProjections[id]?.projectedGames` (not a row field) and `computeConsistency(careerStats, id)` (a pure function, safe to call per tab) are the two traps. Mountable from any surface; `openPlayerDetail(id)`'s signature lets `Market.jsx`/`Portfolio.jsx` open it with no changes of their own |
| `roster/{MyTeamView,PlayerCard,Sparkline}.jsx` | Unimported by `App.jsx`; kept on disk and compiled by `shell/importIntegrity.test.jsx`. Not dead code — do not sweep |
| `board/Board.jsx`, `trade/Trade.jsx` | Gated placeholders (marginal-value/phase prerequisites); nav labels "Draft board"/"Trade desk" |
